#include <common>

uniform float uDensity;
uniform float uBase;
uniform float uPow;
// varying float atmoPct;
varying vec3 vCameraPosition;
varying vec3 vWPosition;
varying mat4 vModelMatrix;
varying vec3 vaNormal;